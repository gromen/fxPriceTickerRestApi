# REST API using Expressjs and nodejs

`/api/getAllPrices` and `/` - endpoint returns all price records
`/api/getLatestPrice/:id` - endpoint for getting particular record filtered by id

```js
yarn //install dependencies
//then
yarn start //run app
```
